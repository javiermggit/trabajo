import { Injectable } from '@angular/core';
import { environment } from 'src/environments/environment';

@Injectable({
  providedIn: 'root'
})
export class VistahcService {
  //readonly apiUrl = environment.apiUrl;

  // Edita estas funciones con tu lógica real (HTTP, etc.)
  buscarPaciente() {}
  abrirHistoria() {}
}
