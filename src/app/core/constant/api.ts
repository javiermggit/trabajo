import { environment } from "src/environments/environment";

export class APIs {
    static pais = {
        //getParIps: `${environment.apiHC}/api/ParIps/List`,
    }
    static LogoCliente = {
        //getLogoCliente: `${environment.apiHC}/api/Images/GetLogoCliente`,
    }
}